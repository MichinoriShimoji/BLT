# 作業引き継ぎメモ（BLT / Mac間共用）

最終更新: 2026-08-22。

**このファイルの読み方**: ここには「運用ルール」「現在地」「未処理TODO」だけを置く。
過去の経緯はこのリポジトリの git log（コミットメッセージが詳細）を参照。

## 開発と公開の運用ルール（2026-08-21 確立・厳守）

- **開発は常にこのフォルダ** `~/Documents/BLT`（github.com/MichinoriShimoji/BLT、Zenodo DOI 連携）。
- **ウェブサイトへはコピー同期のみ**: `blt.html` を `~/Documents/mshimoji.com/blt.html` にコピーし、
  サイト側リポに「`BLT sync: 〜 (BLT <hash>)`」でコミット＆push（textmap と同方式）。
  **サイト側 blt.html を直接編集しない**（2026-06 に直編集で分岐し、8月に3-wayマージで統合する羽目になった教訓）。
- 別PCで作業したら**必ず両リポジトリを push**。「サイトが更新されない」の原因は
  ほぼ「BLTリポには push したがサイト同期をしていない」か「push 忘れ」（2026-08-21 の事件）。
- サイトの公開URLは **https://michinorishimoji.github.io/mshimoji.com/blt.html**。
  旧ドメイン mshimoji.com は **2026-02 に失効→第三者が取得済み**（不審サイトへ転送）。買い戻さない方針。
  公開リンクは元々 github.io なので実害なし。サイト内の旧ドメインリンクは除去済み。

## 動作検証の方法

- ローカルプレビュー: `node ~/Documents/mshimoji.com/.claude/serve.js` → http://localhost:8753/
  （blt.html を配信。Claude のブラウザ検証もこれを使用）。
- 機能テストは**内蔵デモ音声**で: 「新規に始める」→「▶ デモ音声で試す（2チャンネル）」→「自動検出を実行」。
  2話者（A=6.5秒/B=12.1秒）のマルチチャンネル一式が試せる。

## 現在地（2026-08-21 時点の主な機能）

v1.0.2 以降にこの日までで入ったもの（BLT repo HEAD = サイト同期済み）:

- **マルチチャンネル**: 話者ごとの別WAV読込（同時スタート前提・警告あり）、話者別の積み波形、
  チャンネル別自動検出（クロストーク無し）、話者chでの再生/切り出し、EAFに全chをMEDIA_DESCRIPTOR出力、
  話者別ID（ファイル名＋話者内連番）。
- **IPAパレット**: 常設（フォーカスで開閉しない。↓最小化のみ手動）、**ポジティブリスト**
  （✎記号を選ぶ→選んだものだけ表示、空=全表示、リセットで全復元、localStorage `pal_keep`）、
  よく使う帯（頻度順・最小化でも常駐、`pal_freq`）、区間ミニ波形スクラバー
  （クリック=そこから再生/ドラッグ=範囲再生・カーソル保持）、Shift+Space 再生/停止。
- **IPAパレット（8/22 拡張）**: **押す順序は自由**（入力先が無ければ見出しバーに「入力待ち」として溜め、
  セルに入った時点で挿入。直前まで編集していたセルがあればそこへ戻して挿入）。
  **正書法（母音・子音）**の合成済み文字と、**合成用（前の字に付ける）**の結合記号を追加。
  合成は NFC で1文字化（`z`＋`◌̌`→`ž`）、合成済みが無い組み合わせ（`ɨ̌` など）は書記素クラスタのまま
  **「作った記号」**グループに登録して常備（localStorage `pal_made`）。
  結合記号のプレビュー土台は**実測で ◌ / o を自動切替**（端末のフォントで ◌ と結合記号が別フォントに割れると
  豆腐になるため。`combBase()`）。U+0335 はどの土台でも出ないので不採用。
- **記号のキーボードショートカット（8/22）**: 修飾キー＋キーで記号を入力（合成用に割り当てると「直前の字に付ける」）。
  既定は macOS のデッドキー慣習に寄せて **⌥E=´ / ⌥U=¨ / ⌥I=ˆ / ⌥N=˜ / ⌥` =` / ⌥V=ˇ / ⌥M=¯ / ⌥. =◌̣**。
  「**⌨ ショートカット**」ボタンで記号→キーの割り当てを自由に変更（Backspaceで解除・Escで終了）。
  キーは **`e.code`** で保持（Mac の Option＋英字は `e.key` が別文字になるため）。**修飾キー無しは一切横取りしない**。
  `Ctrl+Shift+Z` などブラウザ既定（やり直し）と重なる組み合わせは避けること。
- **パレット構成の永続化**: 選んだ記号・作った記号・使用頻度は**プロジェクト .json にも保存**（`palette` キー：keep / made / freq / keys）。
  再開（.json読込）時は復元したうえで**パレットは畳んだ状態**で待機（`applyProjPalette`／「↑ 開く」で開く）。
  新規のときは自動区切り後に**「✎ 表記のためのパレットを作る」カード**（`#cardPal`）で選択へ誘導。
  ツアーにも2ステップ（パレット作成・合成）を追加済み。
- **再生**: 区間末で正確に停止（setTimeoutスケジュール、誤差1〜5ms）。
- **注釈**: 各レコードの▶直下に✕（削除）、層チップのドラッグ並び替え、未保存で閉じる前に警告
  （スナップショット比較、保存/読込で基準更新）。
- **形態素と文末記号**: `text=nkjaan=ca.` → morph表示・検索・辞書見出しは `ca`（dispMorph）。
  **内部のEOS判定（bdr自動コーディング・文数）は生形 `ca.` を使い続ける**（壊さないこと）。
- **見た目**: 8/21 のヘビーメタル刷新（ダークheader・赤アクセント・メタルロゴ）。8/22 にロゴ右の情報プレート
  （ⓘドロップダウン／初期画面カード）を可読性重視で組み直し（明朝→太いサンセリフ、頭文字B・L・Tは明るいメタル、
  行全体をロゴと同じ skewX(-4deg)）。**ロゴのホバー演出は復活**（文字を赤クロームに点火＋吹き出し。
  地はベタ濃色・文字はベタ白＝クローム文字は小さいと読めないため使わない）。メッセージは data-word / data-sub /
  data-sub-en 属性で持つ（BREAKING THE SILENCE・LIVING AFTER GLOSSING・TIER OF THE BEAST）。
- パネル1整理（既存再開で②保存ファイルを区切り表示、デモは新規のみ）、日英トグルは新UIも対応済み。

## 未処理TODO・保留

- **EAF/TSV/JSON書き出しの morph 欄は生形（`ca.`）のまま**（意図的: EAF往復で bdr 自動判定を
  保全するため）。書き出しも `ca` に揃えたい要望が出たら、bdr情報の保持方法とセットで対応。
- マルチチャンネルは**同時スタート前提**。録音開始ズレの手動オフセット補正は未実装（要望が出たら）。
- 基礎語彙調査向けプリセット（`item`調査ID層＋`type`種別ドロップダウン層）を提案済み・未実装（返答待ち）。
- バージョン表示（ヘッダーに v日付）を提案済み・未実装。
- 旧見出し（`ca.` 等）で保存済みプロジェクトの語義（lexicon）はキー不一致になる場合あり（再入力で解消）。
