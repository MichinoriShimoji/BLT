# 作業引き継ぎメモ（BLT / Mac間共用）

最終更新: 2026-08-22。

**このファイルの読み方**: ここには「運用ルール」「現在地」「未処理TODO」だけを置く。
過去の経緯はこのリポジトリの git log（コミットメッセージが詳細）を参照。

## 開発と公開の運用ルール（2026-08-21 確立・厳守）

- **開発は常にこのフォルダ** `~/Documents/BLT`（github.com/MichinoriShimoji/BLT、Zenodo DOI 連携）。
- **ウェブサイトへはコピー同期のみ**: `blt.html` を `~/Documents/mshimoji.com/blt.html` にコピーし、
  サイト側リポに「`BLT sync: 〜 (BLT <hash>)`」でコミット＆push（textmap と同方式）。
  **サイト側 blt.html を直接編集しない**（2026-06 に直編集で分岐し、8月に3-wayマージで統合する羽目になった教訓）。
- 別PCで作業したら**必ず両リポジトリを push**。「サイトが更新されない」の原因は
  ほぼ「BLTリポには push したがサイト同期をしていない」か「push 忘れ」（2026-08-21 の事件）。
- サイトの公開URLは **https://michinorishimoji.github.io/mshimoji.com/blt.html**。
  旧ドメイン mshimoji.com は **2026-02 に失効→第三者が取得済み**（不審サイトへ転送）。買い戻さない方針。
  公開リンクは元々 github.io なので実害なし。サイト内の旧ドメインリンクは除去済み。

## 動作検証の方法

- ローカルプレビュー: `node ~/Documents/mshimoji.com/.claude/serve.js` → http://localhost:8753/
  （blt.html を配信。Claude のブラウザ検証もこれを使用）。
- 機能テストは**内蔵デモ音声**で: 「新規に始める」→「▶ デモ音声で試す（2チャンネル）」→「自動検出を実行」。
  2話者（A=6.5秒/B=12.1秒）のマルチチャンネル一式が試せる。

## 現在地（2026-08-21 時点の主な機能）

v1.0.2 以降にこの日までで入ったもの（BLT repo HEAD = サイト同期済み）:

- **マルチチャンネル**: 話者ごとの別WAV読込（同時スタート前提・警告あり）、話者別の積み波形、
  チャンネル別自動検出（クロストーク無し）、話者chでの再生/切り出し、EAFに全chをMEDIA_DESCRIPTOR出力、
  話者別ID（ファイル名＋話者内連番）。
- **IPAパレット**: 常設（フォーカスで開閉しない。↓最小化のみ手動）、**ポジティブリスト**
  （✎記号を選ぶ→選んだものだけ表示、空=全表示、リセットで全復元、localStorage `pal_keep`）、
  よく使う帯（頻度順・最小化でも常駐、`pal_freq`）、区間ミニ波形スクラバー
  （クリック=そこから再生/ドラッグ=範囲再生・カーソル保持）、Shift+Space 再生/停止。
- **パレットは「明示的に開いたときだけ」表示（8/23）**: 起動・読み込み・再開のどこでも自動表示しない。
  入り口は「⌨️ 特殊記号の入力補助」カードの2ボタン（⚙設定モード／通常挿入）。「✕ 閉じる」で完全に隠れる
  （閉じるとき ⚙/✎ モードも抜ける）。最小化(palMin)概念は廃止。ショートカットはパレットが閉じていても効く。
- **波形（パネル3）は常時表示（8/23）**: 自動折りたたみ（集中モード）・自動コンパクト(compact3)を廃止。
  「－ 折りたたむ／＋ 波形をひらく」の1ボタン（`setWaveMin`）だけが明示的に畳む。畳み中は見出しクリックでも開く。
- **書き出しカード（旧パネル5）は非表示**: ヘッダー保存バーに LaTeX(gb4e)・Word(.docx)・小型大文字トグルを追加し
  全書き出しを集約（hdr* → 隠しカード内の dl* を代理クリック。DOMは残す）。ツアーの書き出しステップは #saveBar へ。
- **IPAパレット（8/22 拡張）**: **押す順序は自由**（入力先が無ければ見出しバーに「入力待ち」として溜め、
  セルに入った時点で挿入。直前まで編集していたセルがあればそこへ戻して挿入）。
  **正書法（母音・子音）**の合成済み文字と、**合成用（前の字に付ける）**の結合記号を追加。
  合成は NFC で1文字化（`z`＋`◌̌`→`ž`）、合成済みが無い組み合わせ（`ɨ̌` など）は書記素クラスタのまま
  **「作った記号」**グループに登録して常備（localStorage `pal_made`）。
  結合記号のプレビュー土台は**実測で ◌ / o を自動切替**（端末のフォントで ◌ と結合記号が別フォントに割れると
  豆腐になるため。`combBase()`）。U+0335 はどの土台でも出ないので不採用。
- **記号のキーボードショートカット（8/22）**: 修飾キー＋キーで記号を入力（合成用に割り当てると「直前の字に付ける」）。
  既定は **⌃⌥（Ctrl＋Option）**: ⌃⌥E=´ / ⌃⌥U=¨ / ⌃⌥I=ˆ / ⌃⌥N=˜ / ⌃⌥`=` / ⌃⌥V=ˇ / ⌃⌥M=¯ / ⌃⌥.=◌̣。
  **⌥単独は使わないこと**（macOSが先に処理する: ⌥Z→Ω、⌥I→デッドキーのˆ、日本語IMEだと入力モードまで変わる。
  割り当て時も ⌥ 単独は拒否する）。Ctrl+J/K/L は日本語IMEのかな/カナ/英数に当たるので既定に入れない。
  IME変換中（`isComposing`／keyCode 229）は横取りしない。**記号そのもの（ž など）にもキーを割り当てられる**。
  **有効な欄は text と udl（morphの分割元）だけ**（`palKeyTarget()`。gloss・pos・trans 等の注釈欄では無効＝
  略語入力と衝突させない。パレットのクリック挿入はどの欄でも可）。
  「**⚙ ショートカット**」モード中は**記号をクリックすると、その記号のそばにポップ**（`#palKeyPop`・fixed）が出て
  キーを待つ。押せば式（`ž ＝ ⌃＋⌥＋Z`）＋✓を見せて自動で閉じる（1.4s）。警告（⌥単独・修飾なし）や
  Backspace解除もポップ内。パレット上部は細いバー（説明1行＋割り当てチップ✕）。外側クリックでポップは閉じる。
  割り当てた記号は（絞り込み中でも）必ずパレットに載せる。
  キーは **`e.code`** で保持（Mac の Option＋英字は `e.key` が別文字になるため）。**修飾キー無しは一切横取りしない**。
  `Ctrl+Shift+Z` などブラウザ既定（やり直し）と重なる組み合わせは避けること。
- **パレット構成の永続化**: 選んだ記号・作った記号・使用頻度・ショートカットは**プロジェクト .json にも保存**
  （`palette` キー：keep / made / freq / keys）。再開（.json読込）時は復元したうえで**パレットは畳んだ状態**で待機
  （`applyProjPalette`／「↑ 開く」で開く）。
- **入力補助の導線（8/23 確定形）**: 音声読み込み直後は**そのまま区間づくりへ自動スクロール**（設定ステップは廃止。
  ウィザード `#cardPal` は撤去済み）。かわりに**注釈表の直上に「⌨️ 特殊記号の入力補助」カード**（`#cardSymHelp`・
  赤枠で目立たせる・読み込みで表示）を置き、**⚙ ショートカットキーを設定**（パレットを開いて⚙モード＝記号クリック→
  その場のポップでキーを押すだけ）と**記号パレットから直接入力**（通常モードで開く）に誘導。開くとき一瞬
  ハイライト（`.attn`）。ツアーは text/trans の直後にこのカードのステップを配置。
- **再生**: 区間末で正確に停止（setTimeoutスケジュール、誤差1〜5ms）。
- **注釈**: 各レコードの▶直下に✕（削除）、層チップのドラッグ並び替え、未保存で閉じる前に警告
  （スナップショット比較、保存/読込で基準更新）。
- **形態素と文末記号**: `text=nkjaan=ca.` → morph表示・検索・辞書見出しは `ca`（dispMorph）。
  **内部のEOS判定（bdr自動コーディング・文数）は生形 `ca.` を使い続ける**（壊さないこと）。
- **見た目**: 8/21 のヘビーメタル刷新（ダークheader・赤アクセント・メタルロゴ）。8/22 にロゴ右の情報プレート
  （ⓘドロップダウン／初期画面カード）を可読性重視で組み直し（明朝→太いサンセリフ、頭文字B・L・Tは明るいメタル、
  行全体をロゴと同じ skewX(-4deg)）。**ロゴのホバー演出は復活**（文字を赤クロームに点火＋吹き出し。
  地はベタ濃色・文字はベタ白＝クローム文字は小さいと読めないため使わない）。メッセージは data-word / data-sub /
  data-sub-en 属性で持つ（BREAKING THE SILENCE・LIVING AFTER GLOSSING・TIER OF THE BEAST）。
- パネル1整理（既存再開で②保存ファイルを区切り表示、デモは新規のみ）、日英トグルは新UIも対応済み。

- **Word書き出し（8/23）**: 全層がインターリニアの表内に収まる。語揃え行＝surface/text/gloss＋**gloss以外の
  morph層（bdr/wbdr除く）**、全幅行（`wCellSpan`＝gridSpan・折返し可）＝**trans＋区間層（morphSrcを除く、
  「名前: 値」表記）**を最終チャンクの表に追加。列幅見積りは追加行も考慮。`alignIGT(s,f)` は層名を取れるよう汎用化
  （既定 gloss＝gb4e等の既存呼び出しは無変更）。

- **境界層とプリセットの分離（8/23 バグ修正）**: bdr/wbdr は**プリセットの署名に数えない**（`detectPreset` は
  境界層を除いた core で判定）。プリセット切替時も**境界層は維持**し、消失確認の対象にもしない（自動生成のため）。
  以前は文境界をチェックした瞬間に層構成が「カスタム」に落ちて戻れなくなるスタックがあった。

- **見た目のスキン切替（8/23）**: ヘッダーの `</> Tech` / `🤘 Rock` ボタン（`#skinBtn`）で
  **テック調（既定）⇄ ロック調**を切替（既定は `<html data-skin="tech">`。ロック調＝属性なし。
  `<head>` の先行スクリプトで保存値（localStorage `blt_skin`／`blt_theme`）を描画前に反映＝チラつき無し）。`html[data-skin="tech"]` の1ブロック＋CSS変数だけで完結し、
  ライト/ダーク（`data-theme`）とは直交（`html[data-skin="tech"][data-theme="dark"]` を後ろに置いて解決）。
  テック調＝端末/エディタ風: 緑アクセント・ヘアライン・角丸3-4px・グラデーション/グロー無し・等幅の見出し、
  ロゴは**クロームSVGを隠して `$ BLT█`（点滅カーソル付きの等幅テキスト `.tlogo`）**に差し替え、
  ブランドカードはダイヤモンドプレート→**ドットグリッド**。B/L/T のホバー文言もテック版を持つ
  （`data-word` は SVG path と `.tlogo b` の両方に付くので、吹き出しJSのセレクタは `[data-word]`）。
  設定は localStorage `blt_skin`。**機能・データには一切影響しない**（見た目のみ）。

- **削除ボタンの誤爆対策（8/24）**: 注釈表の各行で、▶再生と✕削除が5px しか離れておらず押し間違いの危険があった。
  **間隔を26pxに広げ（境目に細線）、常時赤（`--danger`）**にした。`--danger` は4配色ブロック
  （既定／ダーク／テック／テック・ダーク）に定義。

- **入力直後のボタン空振り修正（8/24）**: セル編集 → 別行の▶などを押すと1回目が反応しない問題。
  原因は **blur（mousedown時）の再描画でボタン要素が差し替わり、mousedown と mouseup の相手が変わって
  click が成立しない**こと（特に bdr/wbdr 有効時は `renderTable()` が走る）。対策は `rerenderSoon()`:
  **ポインタを押している間は再描画を保留し、離して click が確定してから実行**（pointerup で `setTimeout(...,0)`。
  取りこぼし保険に800msのフォールバック）。保留中に行が消えるとズレるので、
  グロス行の差し替えは `segs[i]!==s` なら表ごと引き直す。モデル更新（値の確定・computeBoundaries）は従来どおり即時。

- **画面が跳ねる問題の再発修正（8/24）**: text欄をクリックした瞬間／入力確定して再生ボタンを押した瞬間に
  ウィンドウが下へ動く件。原因は2つ:
  (1) `renderContext()` 末尾の `cur.scrollIntoView({block:'nearest'})` が**ウィンドウごと**動かしていた
  → `afterSelChange` と同じ**枠内 scrollTop 計算**に統一（`scrollIntoView` は使わない）。
  (2) セル編集開始時（`document` の focusin）の「隠れ防止スクロール」が、波形パネルを常時表示にしたことで
  高くなった sticky 下端を基準に頻繁に発動していた → **ポインタ操作から400ms以内＝クリックで入った場合は
  スクロールしない**（利用者が見えている位置を選んでいるため）。Enter移動など鍵盤操作のときだけ従来どおり追従。

- **コーパス（複数プロジェクトの横断集計・8/24）**: 分析・辞書タブで保存済み .json を複数読み込み、
  **読み取り専用**で集計・形態素リスト・辞書用例を横断表示（`corpus[]` / `corpusRecs[]`）。設計方針は
  **「統合」ではなく「束ねる」**（1プロジェクト=1音声の前提を壊さない。時刻の混在・ID衝突・話者名衝突を原理的に回避）。
  - 形態素の切り出しは必ず **`withSession()` でそのセッション自身の設定**（morphSrc・morphSeps・bdrSeps・tiersDef）に
    差し替えて評価し、必ず元へ戻す。**規則の取り違えを防ぐ核心部分なので触らないこと**。
  - 用例は「セッション名 › ID」を表示。**再生・注釈へジャンプは出さない**（他セッションの音声を持たないため）。
  - 語義はセッションごとに併記し、**食い違いは警告するだけで自動統合しない**。語義編集欄と辞書ZIPは無効。
  - 重複追加は `sessionId` で検出。保存フォーマットに **`sessionId`（UUID）と `audio` 指紋（名前/長さ/レート/バイト数）**を追加済み
    （将来の「同一音声の統合」の土台。既存ファイルは読込時に採番）。
  - **検索タブも横断対応（8/24）**: 条件の層は全セッションの**和集合**（片方にしか無い層も選べる）、
    判定は `withSession()` でセッションごと、KWICの前後文脈は**同一セッション内**に限定、
    結果は「セッション名 › ID」表示で**注釈タブへのジャンプは無効**（他セッションの音声・注釈は開けないため）。
  - **辞書ZIP（HTML＋音声）も横断対応（8/24・案b採用）**: コーパスバーにセッション一覧を出し、
    **セッションごとにWAVを添付**（`sess.wav` / 複数chは `sess.tracks[話者]`。複数chは `trackFiles[].name` で
    ファイル名を突き合わせ、合わない分は順番で補い、相違はダイアログで知らせる）。
    書き出しは各レコードを**出典セッションのWAV**から切り出し、クリップ名は `セッション名_連番.wav`。
    **未添付のセッションは警告のうえ音声なしで書き出す**（そのレコードは `clip:''` → 辞書HTMLで再生ボタンを出さない）。
    辞書HTMLは用例に「セッション名 › ID」を表示し、見出し部にコーパスの内訳行を出す。
    語義が食い違う見出しは `sessionA: 母親 ／ sessionB: お母さん` のように**出典つきで併記**（1つに潰さない）。

## 未処理TODO・保留

- **EAF/TSV/JSON書き出しの morph 欄は生形（`ca.`）のまま**（意図的: EAF往復で bdr 自動判定を
  保全するため）。書き出しも `ca` に揃えたい要望が出たら、bdr情報の保持方法とセットで対応。
- マルチチャンネルは**同時スタート前提**。録音開始ズレの手動オフセット補正は未実装（要望が出たら）。
- 基礎語彙調査向けプリセット（`item`調査ID層＋`type`種別ドロップダウン層）を提案済み・未実装（返答待ち）。
- バージョン表示（ヘッダーに v日付）を提案済み・未実装。
- 旧見出し（`ca.` 等）で保存済みプロジェクトの語義（lexicon）はキー不一致になる場合あり（再入力で解消）。
